const numeros = [8, 11, 4, 1];

let diferenca = 0
for (let i = 0; i <= numeros.length; i++) {
    if (diferenca = Math.max(...numeros)-Math.min(...numeros)){
        console.log(diferenca)
        break;
    }
};